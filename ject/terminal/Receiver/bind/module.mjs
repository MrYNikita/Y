//#region YI

/** @type {import('./config.mjs')['default']?} */
let config = null;

await import('./config.mjs')

    .then(c => config = c.default)
    .catch(e => e);

//#endregion
//#region YT

/** ### bindT
 * - Тип `T`
 * - Версия `0.0.0`
 * - Модуль `ject\terminal\receiver\bind`
 *
 * Основной параметр модуля `bind`.
 *
 * @typedef bindT
 * @prop {any} _
 *
*/

/** ### bindTTBind
 * - Тип `TT`
 * - Версия `0.0.0`
 * - Модуль `ject\terminal\receiver\bind`
 *
 *
 *
 * @typedef {[string, boolean, boolean, function(TA):void]} bindTTBind
 * @template TA
*/

//#endregion
//#region YV



//#endregion

/**
 * @file module.mjs
 * @author Yakhin Nikita Artemovich <mr.y.nikita@gmail.com>
 * @copyright Yakhin Nikita Artemovich 2023
*/